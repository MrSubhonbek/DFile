import React from "react";
import s from './NavbarIcon.module.scss'

interface IProps {

}

export const NavbarIcon = (props: IProps) => {
    return (
        <div className ={s.navbarIcon}></div>
    )
}

